{
	"scripts": {
		"dev": "esbuild js/*.jsx --minify --format=esm --bundle --outdir=src/tldraw/static --sourcemap=inline --watch",
		"build": "node node_config.mjs"
	},
	"dependencies": {
		"@anywidget/react": "^0.2.2",
		"@tldraw/tldraw": "^5.0.1",
		"react": "^19.2.6",
		"react-dom": "^19.2.6"
	},
	"devDependencies": {
		"esbuild": "^0.28.0"
	}
}
