{
	"scripts": {
		"dev": "npm run build -- --sourcemap=inline --watch",
		"build": "esbuild js/*.jsx --minify --format=esm --bundle --outdir=src/tldraw/static"
	},
	"dependencies": {
		"@anywidget/react": "^0.0.2",
		"@tldraw/tldraw": "^2.0.0-canary.b9d82466295e",
		"react": "^18.2.0",
		"react-dom": "^18.2.0"
	},
	"devDependencies": {
		"esbuild": "^0.19.5"
	}
}
